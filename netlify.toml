[build]
  command = "npm run build"
  publish = "dist"

# Single served page — send any unknown path back to the entry document.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
